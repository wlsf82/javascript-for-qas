const firstName = 'Walmyr'
const lastName = 'Filho'

console.log(firstName + ' ' + lastName)
console.log(`${firstName} ${lastName}`)

const name = 'Acme'
const size = 'Large'
const employees = 240
console.log(`The client ${name} is ${size} with ${employees} employees`)

const customerId = 42
console.log(`/customers/${customerId}`)

console.log(`walmyr+${Date.now()}@example.com`)
